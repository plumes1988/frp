<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.figure.core.repository.visual.VisualMediaMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="com.figure.core.model.visual.VisualMedia">
                <result column="id" property="id" />
                <result column="name" property="name" />
                <result column="type" property="type" />
                <result column="size" property="size" />
                <result column="resolution" property="resolution" />
                <result column="length" property="length" />
                <result column="url" property="url" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="Base_Column_List">
                id,
            name, type, size, resolution, length, url
        </sql>

</mapper>
