<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.figure.core.notificationEntranceData.mapper.NotificationEntranceDataMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="com.figure.core.model.notification.NotificationEntranceData">
                <result column="id" property="id" />
                    <result column="event" property="event" />
                    <result column="user" property="user" />
                    <result column="sendMode" property="sendMode" />
                    <result column="msg" property="msg" />
                    <result column="status" property="status" />
                    <result column="creationTime" property="creationTime" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="Base_Column_List">
                id,
            event, user, sendMode, msg, status, creationTime
        </sql>

</mapper>
