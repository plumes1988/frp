<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.figure.core.repository.signal.SignalMatrixInfoMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="com.figure.core.model.signal.SignalMatrixInfo">
                    <id column="matrixId" property="matrixId" />
                    <result column="matrixName" property="matrixName" />
                    <result column="deviceId" property="deviceId" />
                    <result column="logicChannelCode" property="logicChannelCode" />
                    <result column="inputCount" property="inputCount" />
                    <result column="outputCount" property="outputCount" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="Base_Column_List">
            matrixId, matrixName, deviceId, logicChannelCode, inputCount, outputCount
        </sql>

</mapper>
